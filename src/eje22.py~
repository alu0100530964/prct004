#!/usr/bin/python
#!encoding: UTF-8

a = float(raw_input('Valor de a: '))
b = float(raw_input('Valor de b: '))

if a != 0:
	x = -b/a
	print 'Solucion: ', x
if a = 0:
	print 'La ecuación no tiene solución.'

# El error que se produce en tiempo de compilacion es que le estas asignando el valor 0 a la variable "a" y arriba le estas pidiendo que le des un valor cualquiera

